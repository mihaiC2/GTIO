{
    "name": "voting-system",
    "private": true,
    "scripts": {
        "install": "npm install --prefix auth-service && npm install --prefix user-service && npm install --prefix singer-service && npm install --prefix vote-service && npm install --prefix api-gateway && npm install --prefix shared",
        "dev": "concurrently \"npm run dev --prefix auth-service\" \"npm run dev --prefix user-service\" \"npm run dev --prefix singer-service\" \"npm run dev --prefix vote-service\" \"npm run dev --prefix api-gateway\""
    },
    "devDependencies": {
        "concurrently": "^7.0.0"
    },
    "dependencies": {
        "ts-node-dev": "^2.0.0"
    }
}
